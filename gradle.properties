# The build rewrites build/libs and build/classes in place (the ProGuard + resource-pack step), and Gradle's
# file-system watcher can keep a handle on those outputs between builds on Windows, leaving a stale jar that
# the next build reads. Disabling watching keeps consecutive builds deterministic.
org.gradle.vfs.watch=false
